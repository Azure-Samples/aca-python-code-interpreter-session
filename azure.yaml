name: semantic-kernel-sessions
infra:
  provider: bicep
services:
  chat-api:
    project: ./
    host: containerapp
    language: python
    docker:
      path: Dockerfile
      context: ./
metadata:
  template: aca-python-code-interpreter-session@0.0.1-beta
